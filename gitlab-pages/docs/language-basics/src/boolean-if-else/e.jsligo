const a: tez  = 5mutez;
const b: tez  = 10mutez;
const c = (a == b); // false
type magnitude = ["Small"] | ["Large"]; // See variant types.

const compare = (n) => {
  if (n < 10n) return Small() else return Large()
};