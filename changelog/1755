author: melwyn95
description: '- `--protocol jakarta` now emits `SUB_MUMAV` for subtraction `mutez`
  values'
merge_request: '1755'
title: 'fix: --protocol jakarta emits `SUB` for mutez subtraction'
type: fixed
