author: melwyn95
description: '- `--protocol jakarta` now emits `SUB_MUMAV` for subtraction `mumav`
  values'
merge_request: '1755'
title: 'fix: --protocol jakarta emits `SUB` for mumav subtraction'
type: fixed
