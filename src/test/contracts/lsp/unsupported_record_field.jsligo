type storage = { sponsee: address, ledger: big_map<address, mav> }

type ret = [list<operation>, storage]

@entry
const sponsorise = (delta: int, store: storage): ret => {
  return [list([]), { ...store, 1 }]
}
