const f = "../../../../../src/contract/unit.mligo"

const originate = () : address => {
    let [c_addr,_code,_size] = Test.originate_from_file(f, "main", list([]), Test.eval(unit), 0 as mutez);
    return c_addr;
}