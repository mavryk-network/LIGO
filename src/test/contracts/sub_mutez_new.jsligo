const sub = ([store, delta] : [mav, mav]) : option<mav> => store - delta;

const main = ([_, store] : [unit, mav]) : [list<operation>, mav] => {
 return [ (list([]) as list <operation>), Option.unopt(sub ([store, 1 as mav])) ]
};
