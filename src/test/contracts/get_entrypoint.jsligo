@entry
const main = (_u : unit, _b : address) : [list <operation>, address] => {
  let c : contract<int> = Option.unopt(Mavryk.get_entrypoint_opt ("%foo", Mavryk.get_sender()));
  return [list([]) as list <operation>, Mavryk.address(c)];
};
