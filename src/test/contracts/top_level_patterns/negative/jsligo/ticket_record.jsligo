type t = { b : ticket<string> }

const { b } = { b : Option.unopt(Mavryk.create_ticket("one", 10 as nat)) }

type storage = ticket<string>

const main = (_ : unit, _ : storage) : [list<operation>, storage]
  => [list([]), Option.unopt (Mavryk.join_tickets (b, b))]